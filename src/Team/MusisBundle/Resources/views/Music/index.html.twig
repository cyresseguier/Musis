{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Music list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Album</th>
                <th>Year</th>
                <th>Link</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('music_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.album }}</td>
                <td>{{ entity.year }}</td>
                <td>{{ entity.link }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('music_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('music_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('music_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
